const plannerState = {
  planner: {
    selectedPlanner: null,
  },
};

const plannerReducer = (state = plannerState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default plannerReducer;
