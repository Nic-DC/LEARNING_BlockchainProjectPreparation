const taskState = {
  task: {
    selectedTask: null,
  },
};

const taskReducer = (state = taskState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default taskReducer;
