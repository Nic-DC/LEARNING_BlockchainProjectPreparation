const utilsState = {
  sidebar: {
    selectedPlanner: null,
  },
};

const utilsReducer = (state = utilsState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default utilsReducer;
