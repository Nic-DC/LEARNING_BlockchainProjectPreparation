const testState = {
  cart: {
    content: [],
  },
};

const testReducer = (state = testState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default testReducer;
